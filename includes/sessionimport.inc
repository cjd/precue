<?PHP

//for convenience we import our
//session variables. This is still
//much safer than using register_globals
//because we know the source.
$bible = $_SESSION['bible'];
$biblename = $_SESSION['biblename'];

$media = $_SESSION['media'];
$media_path = $_SESSION['media_path'];
$themeset = $_SESSION['themeset'];

?>
